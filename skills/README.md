The `wavespeed/SKILL.md` here is the same skill the CLI ships in
[WaveSpeedAI/wavespeed-cli `skills/SKILL.md`](https://github.com/WaveSpeedAI/wavespeed-cli/blob/main/skills/SKILL.md)
(what `wavespeed skill install` writes for Claude Code / Cursor / Codex).
When updating, change it there first and copy it here verbatim.
